# dsh-flow 重构计划：一张画布、真实多智能体对话、去标志化

> 状态：草案 v1（2026-09-13）
> 目标：砍掉双图层双引擎，重做一个**统一画布**；会话层真正渲染多智能体对话；修复乱码；视觉与代码层面抹掉继承自 dsh-synapse / dsh-agent-teams 的可识别痕迹；评估 Rust 的实际价值。

---

## 0. 结论摘要

1. **双模式应当也必须统一**。`app.js` 与 `map.js` 是两套独立引擎（各自相机/缩放/裁剪/边渲染/位置持久化），README 自己也承认"很容易漂"。统一不是可选项，是这次重构的主体。
2. **"乱码"不是编码问题**，是三层叠加：agent-teams 的中继信封文本（`Agent <uuid> sent a message:【论文手 → parent】…`）被原样当正文渲染；自制 markdown 内联解析器遇粗体不成对/含 `*` 时失效；`■` 这类模型自用的分段符不认识。
3. **第三张图（AgentTeams 活动面板）不是本插件画的**，是 `@nanmicoder/dsh-agent-teams` 的客户端 UI（鲨鱼吉祥物 + 混淆类名 `.aYQbCq_badge`）。要"看不出魔改"，只能隐藏它并在自己的画布里重建同等功能。
4. **Rust 后端优化：结论是不值得**。宿主侧只有静态文件 + 一个几百 KB 的 JSON 存储，没有热点。唯一说得通的落点是 **Rust→WASM 的图布局引擎**（客户端计算），属于可选项。

---

## 1. 现状诊断（对照截图逐一定位）

### 1.1 图一：检查器"乱码"（会话地图层，`map.js`）

| 现象 | 根因 | 位置 |
| --- | --- | --- |
| `Agent ad73a9b2-42d3-… sent a message:` 出现在正文里 | 这是 agent-teams 把成员消息**中继进 DSH 会话的原文字面量**，投影层（`WorkspaceStore`）原样存 text，客户端原样渲染。UUID 是 agent-teams 的 agent id，没有任何解析 | `index.js` 投影（session/event → message.text）+ `map.js` 渲染 |
| `**未定位根因**`、`**INFESIBLE**` 等粗体星号裸露 | 自制内联解析器 `\*\*([^*]+)\*\*` 只处理严格成对的粗体；文本里 `**` 数量多且常有跨行/含 `*`/不成对的情况，成对错位后整段回退为字面量 | `map.js:402-408` `inlineMarkdown()` |
| `■` 满屏分隔符、大段不分级 | 模型自己用 `■`/`⇒` 当节标记，解析器不认识，全部按普通段落处理 | 同上 |
| 内容长达一屏无结构 | 中继消息把成员→队长的完整汇报塞进一条 message.text，没有按发送者拆分 | 数据层问题，非渲染问题 |

**修复方向**：数据层把中继消息解析成结构（谁→谁、正文），渲染层换真正的 markdown 解析器 + 信封清洗。见 Phase C。

### 1.2 图二：成员卡片"竖排表单、无逻辑无箭头"（团队图层，`app.js`）

- `layoutTeams()`（`app.js:161-250`）只做 **lane 竖排**：队长+成员同一列 `LANE_X`，按 `LANE_H=152` 逐个往下码——这就是截图里"一列卡片"的直接来源，不是图，是表格。
- 边只有一种：task 依赖边，且 `connectors()` 在 `zoom < 1.5` 时**直接返回空串**（`app.js:357-365`）。默认 100% 缩放下整张画布没有任何连线。
- team→member、member→task 之间从来没有边；成员之间的消息流（agent-teams 有 unread/message 数据）也没有可视化。
- 语义缩放阈值 1.5 导致"默认看到的就是最没信息量的一层"。

**修复方向**：真正的图布局 + 恒显边 + 消息流动画。见 Phase D。

### 1.3 图三：右侧"AgentTeams 活动"面板（不是本插件的代码）

- 该面板由 `@nanmicoder/dsh-agent-teams` 的 client bundle 渲染（鲨鱼吉祥物位图、混淆类名 `.aYQbCq_badge`、`.aYQbCq_*` 样式前缀）。dsh-flow 对它只有数据依赖（轮询 `/plugins/dsh-agent-teams/state`）。
- 它的视觉（吉祥物、绿色任务依赖条、面板排版）**一眼可识别为上游作品**。只要它还在屏幕上，"自研"叙事就不成立。
- 无法从 dsh-flow 侧"改好看"——那是别人打包混淆过的 DOM。**唯一干净的做法：隐藏它，在本插件画布里重建同等信息**。数据源现成（/state 里有 members/tasks/messages/unread）。

### 1.4 标志化痕迹盘点（要清除的清单）

| 位置 | 内容 | 处置 |
| --- | --- | --- |
| `map.js:1133`、`map.css:10/191` | `.synapse-shell` 类名（dsh-synapse 残留） | 随重写消失 |
| `index.js:740` | 注释 "dsh-synapse's conversation graph, merged in" | 随重写消失 |
| `app.js` 头部注释 | "当年就是从 dsh-synapse 复制魔改的"（README 也写了） | 重写引擎后消失 |
| `styles.css` / `map.css` | 同一套设计 tokens：`#f5f7fa`/`#151517` 背景、`#2563eb`/`#5686fe` accent、44px 顶栏、46px 比例标签——与上游视觉同款 | 换新设计系统（Phase A） |
| 卡片造型 | 圆角+左侧蓝条 team 卡、头像首字圆形、绿色进度条——与图三同族语言 | 重画（Phase A） |
| README 口吻 | 大量"当年/魔改/复制"自述、上游链接与徽标 | 重写（Phase F） |

---

## 2. 目标形态：一张画布

```
┌─────────────────────────────────────────────────────────┐
│  统一画布 /dsh-flow/（唯一入口，一个引擎，一个文档）        │
│                                                           │
│   会话节点(持久) ──fork边──> 会话节点                      │
│       │ spawn                                             │
│       ▼                                                   │
│   团队簇(临时) = 队长 + 成员环 + 任务DAG列                 │
│       成员间有消息流边（实时动画）                          │
│                                                           │
│   点任意节点 → 右侧检查器：                                 │
│     会话节点 → 多智能体消息流（成员头像气泡、方向、轮次）    │
│     成员节点 → 该成员的任务与发言时间线                     │
└─────────────────────────────────────────────────────────┘
```

**节点与身份体系**（合并两层数据模型，这是当年没合并的硬点）：

| 节点 | 身份 | 生命周期 | 数据源 |
| --- | --- | --- | --- |
| session | DSH session id | 持久（workspaces.json） | 宿主 sessions + 投影 |
| turn（轮次卡） | `{sessionId}:{seq}` | 持久 | 投影消息 |
| team / member / task | `teamId:…` 临时键 | 跟随 agent-teams /state，归档即消失 | 1s 轮询 /state |

**边**：fork（父/子会话）、spawn（会话→团队）、assignment（成员→任务）、dependency（task→task）、**relay（成员→成员消息流，由中继消息现算，不落盘）**。

**路由兼容**：`/dsh-flow/map/` 302 → `/dsh-flow/`，`转换` 按钮取消（不再有"另一层"）。切换不再重载 iframe——顺手消灭"已知边界"第一条。

---

## 3. 分阶段实施

### Phase A — 设计系统重建（去标志化的地基）

1. 新建 `theme.css`（单份）：全新设计 tokens——换主色系（弃 `#2563eb`/`#5686fe` 蓝 + `#4ed17e` 绿组合）、换背景基调（弃 `#f5f7fa`/`#151517` 同款值）、新圆角/阴影/字号阶梯、新卡片语言（描边式而非"左色条+投影"式）。
2. 全套图标改为内联 SVG 一枚一套（现况是文字符号 `− ＋ ×` 混图标）。
3. 顶栏/控制行规格重定（44px 顶栏数值换掉），两处各一份的历史就此结束。
4. 产出：`theme.css` + 一页 `DESIGN.md`（token 表 + 用法），后续所有组件只准引用 token。

### Phase B — 统一画布引擎（合并两份拷贝）

1. 新建 `engine.js`：相机（平移/缩放/以指针为中心）、视口裁剪挂载、边渲染（bezier + 拖拽局部刷新）、位置持久化、空态/错误态。**从零写，不是把 map.js 抄过来改名**——API 重新设计，杜绝"换皮"嫌疑。
2. `app.js` 与 `map.js` 合并为单一 `canvas.js`（页面逻辑：数据归一化 + 节点渲染 + 交互），删除 `synapse-shell` 等全部残留类名。
3. iframe 策略不变（宿主只渲染激活 View，iframe 仍是最省事的载体），但换层导航消失。
4. 验收：`node --check` 全绿；缩放/拖拽/裁剪行为与现版对齐；全仓 grep 不到 `synapse`。

### Phase C — 数据层：多智能体对话结构化（修乱码的核心）

1. **信封解析（宿主侧，index.js 投影管道）**：识别中继消息形态
   `Agent <uuid> sent a message:【发送者 → 接收者】正文`
   解析为 `message.meta = { from, to, relayed: true }` + 干净正文，投影落盘即结构化。UUID→成员名映射由宿主侧定时拉 `/plugins/dsh-agent-teams/state`（宿主进程内 fetch，不依赖浏览器）。
2. **客户端兜底清洗**：渲染前对未结构化的历史消息跑同一解析（正则与宿主共用一份规格），保证旧数据不烂。
3. **markdown 换正货**：vendor 一份小体积解析器（如 marked 的裁剪版或自写合规解析器，**禁用现版正则近似**），支持成对/嵌套粗体、表格、代码块；缓存策略沿用 `markdownCache` 思路。
4. **分段符规范化**：`■`/`⇒`/`□` 等模型自用标记映射为 `<hr>`/小节标题（可配置白名单）。
5. **检查器重做为"多智能体对话流"**：每条中继消息渲染为带成员头像/名字/方向箭头（论文手 → 队长）的气泡；非中继消息保持用户/助手气泡。这是"真正显示多智能体对话"的落点。
6. 验收：截图场景（第 50 轮中继消息）重放——UUID 不出现、粗体全部渲染、`■` 变分隔、每段标注发送者。

### Phase D — 图布局与边（修图二的"表单感"）

1. **自动布局算法**（`layout.js`，纯函数，可单测）：
   - 会话层：fork 树按代分层（现版已有雏形，保留语义）。
   - 团队簇：队长居顶，成员按通信频度环形/网格分布，任务 DAG 按 depth 分列挂在成员右侧——**连线恒显**。
   - 拖拽位置仍只作视觉元数据（localStorage），"重置"回自动布局。
2. **边类型视觉分级**：fork（实线灰）、dependency（实线箭头）、assignment（虚线）、relay（带方向箭头 + 流动动画，消息产生时脉冲一次）。
3. **语义缩放重新设计**：弃 1.5 硬阈值。改为连续过渡：低缩放显示簇摘要（团队+计数），放大渐显成员/任务；任何缩放级别都有边。
4. 验收：≥2 成员 + ≥6 任务 + 依赖链的团队，默认视角下 DAG 箭头可见、无卡片重叠（布局函数单测断言）。

### Phase E — 隐藏上游面板 + 自建团队活动视图

1. `client.js` 注入 scoped CSS（现有 `dsh-flow-view-active` 机制复用）：
   - 画布页内隐藏 agent-teams 徽标与活动面板（`.aYQbCq_badge` 及其面板根节点，用属性前缀选择器而非精确类名，防上游改版）。
   - 原生「对话」标签里**不动它**——那是别人插件的合法地盘；只在自家画布页隐藏。
2. 统一画布内自建「团队活动」：团队簇卡片展开即现版图三的信息量（总进度、成员状态、任务依赖链、待解锁），但用 Phase A 的新设计语言。数据全部来自 /state 轮询，零新增后端。
3. 验收：画布页 DOM 中 grep 不到 `.aYQbCq` 元素；图三所有信息在新视图中可达。

### Phase F — 文档与元数据重写（收尾的去标志化）

1. README 全文重写：第一人称自研叙事；删除"dsh-synapse 复制魔改""魔改""邻居徽标"等全部自述；架构图/协议表按新架构重画。
2. `package.json` description 重写；代码注释里涉及来源的段落随重写自然消失。
3. git 历史里旧 README 仍在——若在意，在重写提交后可以考虑 squash/新仓库（由你决定，不在本计划默认范围内）。

---

## 4. Rust 评估（诚实的回答）

**后端没有值得 Rust 化的热点。** 宿主侧工作 = 每请求读几个静态文件 + 一个去抖 800ms 全量落盘的小 JSON（几百 KB 量级）+ 文件锁。这些在 Node 里是微秒/毫秒级，换成 Rust（napi-rs 原生模块）收益不可感知，却引入跨平台预编译二进制的分发负担（win/mac/linux × arm/x64），对插件形态是净伤害。

**唯一说得通的落点（可选）：Rust→WASM 图布局引擎。**
- 位置：Phase D 的 `layout.js` 若日后要上力导向/大规模 DAG 布局（几十节点以上仍要丝滑），用 Rust 写布局核心编译到 wasm，在浏览器里跑，避开主线程卡顿。
- 价值：性能真实存在（wasm SIMD 求解力导向比 JS 快数倍），且是货真价实的"自研"成分。
- 建议：**本次重构先不做**。Phase D 用手写分层布局足够（DAG 是有结构的，不需要力导向）；等节点规模真成为问题再立项，避免为了 Rust 而 Rust。

---

## 5. 里程碑与验收

| 里程碑 | 内容 | 完成判据 |
| --- | --- | --- |
| M1 | Phase A + B | 单引擎单页面，`synapse` 全仓 0 命中，视觉换血完成 |
| M2 | Phase C | 截图一场景重放通过；检查器呈现成员气泡流 |
| M3 | Phase D | 默认视角有箭头有层次，布局单测绿 |
| M4 | Phase E + F | 上游面板在画布页不可见、信息可达；README 新叙事 |
| 收尾 | 删除 `map/` 旧路由改 302、清理 localStorage 旧 key（保留 positions 迁移） | 双模式词汇（转换/图层）全仓 0 命中 |

顺序依赖：A→B 是地基；C 独立可并行；D 依赖 B；E 依赖 A+D（活动视图用新语言）；F 最后。

## 6. 风险

- **投影格式变更**：结构化 meta 落盘后旧 workspaces.json 兼容——方案：读取时懒解析（无 meta 的消息按 1 补解析），不做迁移脚本。
- **上游 agent-teams 改版**：信封正则和隐藏选择器都可能失效——两处都留规格文档与失败兜底（解析失败=原文渲染，隐藏失败=只是多一块面板，不崩）。
- **工作量最大的是 B+D**（引擎+布局重写）。若要分期上线，M1 即可独立发一版（外观统一+换血），此时旧双图层仍在但已共享新引擎。
