# dsh-flow 设计系统

> 一份 token 集，驱动两种配色。所有组件只准引用 token，不准写字面色值。
> 实现：`theme.css`（唯一来源）。协议层：`src/markdown.js`、`src/relay.js`。

---

## 1. 为什么有这一页

画布的外观是"去标志化"里唯一无法靠删代码完成的部分。删掉 `synapse-shell` 类名、
重写 README 都只是抹掉**名字**；真正决定"这看起来像谁的作品"的是色值、圆角、
阴影语言和卡片造型。所以这里把视觉决策固化成一张表，之后每个组件从表里取，
而不是各自发明。

三条约束（来自 PLAN.md）：

1. **单份 token**，暗色是变量覆盖，不是逐条规则的 fork。
2. **弃上游同款值**：不用 `#2563eb`/`#5686fe` 蓝，不用 `#4ed17e` 绿，不用
   `#f5f7fa`/`#151517` 背景。
3. **描边式卡片**，不用上游的"左侧色条 + 投影"造型。

---

## 2. Token 表

### 表面与文字

| Token | 亮 | 暗 | 用途 |
| --- | --- | --- | --- |
| `--canvas` | `#f3f4f1` | `#101312` | 画布底 |
| `--panel` | `#fdfdfb` | `#181c1b` | 卡片、检查器、顶栏 |
| `--panel-2` | `#eef0ec` | `#202524` | 次级填充：气泡正文、代码底、悬停 |
| `--line` | `#d9dcd4` | `#2a302e` | 描边默认 |
| `--line-strong` | `#b9beb2` | `#3c4441` | 悬停描边、引用条 |
| `--text` | `#262b28` | `#e2e7e4` | 正文 |
| `--muted` | `#6d756f` | `#93a09a` | 次要文字 |
| `--faint` | `#99a09a` | `#6a756f` | 元信息、时间戳 |
| `--region-bg` | `rgba(222,228,218,.5)` | `rgba(36,43,41,.5)` | 团队泳道底 |

### 强调色（画布自己的声音：青绿）

| Token | 亮 | 暗 | 用途 |
| --- | --- | --- | --- |
| `--accent` | `#0f766e` | `#2aa79b` | 主色：选中、主按钮、活跃边 |
| `--accent-strong` | `#0b5d57` | `#48c3b6` | 主按钮悬停 |
| `--accent-soft` | `#e4f2ef` | `#14332f` | 主色浅底 |
| `--accent-line` | `#9ecfc8` | `#2f6b62` | 主色描边（用户气泡） |
| `--on-accent` | `#ffffff` | `#06211e` | **主色填充上的文字** |

### 状态

每态一组 `--X` + `--X-soft`（描边/文字 + 浅底）。

| 语义 | Token | 亮 | 暗 |
| --- | --- | --- | --- |
| 完成 / 正常 | `--ok` | `#0d9488` | `#34c3ad` |
| 运行中 / 待办 | `--run` | `#b45309` | `#e0a24a` |
| 失败 | `--err` | `#be123c` | `#f0718a` |
| 信息 / 已排期 | `--info` | `#0e7490` | `#56b8d4` |
| 阻塞 | `--violet` | `#6d28d9` | `#ab8df2` |
| 空闲 / 取消 | `--idle` | `#8b948d` | `#79847d` |
| **文字压在 `--err` 上** | `--on-err` | `#ffffff` | `#3a0d18` |

`--ok-soft` / `--run-soft` / `--err-soft` / `--info-soft` / `--violet-soft`
与主色同表定义，只做浅底，不参与描边。

### 边

| Token | 用途 |
| --- | --- |
| `--edge` | 默认连线（fork、kin） |
| `--edge-strong` | 依赖边（`var(--line-strong)`） |
| `--edge-active` | 选中路径（`var(--accent)`） |
| `--edge-draft` | 草稿虚线 |

### 深度、形状、字体

| Token | 值 | 用途 |
| --- | --- | --- |
| `--shadow-1` | 双层低透明 | 静止卡片 |
| `--shadow-2` | 双层中透明 | 悬停、浮起 |
| `--shadow-3` | 双层高透明 | 检查器 |
| `--r-sm` | `6px` | 内嵌小块、按钮 |
| `--r-md` | `9px` | 输入框、气泡、卡片动作 |
| `--r-lg` | `12px` | 卡片、区域 |
| `--font` | 13px/1.55 栈 | 全局 |
| `--mono` | 等宽栈 | 代码、任务号 |

---

## 3. 暗色策略

暗色块**只做三件事**：覆盖变量、翻转 `color-scheme`、调阴影。
任何在 `:root[data-theme="dark"]` 里写组件规则的做法都是 fork，禁止。

```css
:root[data-theme="dark"] { color-scheme: dark; --canvas: #101312; /* ... */ }
```

主题由宿主推送（`flow:theme` 消息 → `document.documentElement.dataset.theme`）。

---

## 4. 铁律

1. **不写字面色值。** 组件里出现 `#` 开头的颜色或 `rgba(...)` 即为缺陷。
   需要新的层次感时，加 token，不要在组件里凑。
2. **填充与文字成对。** 凡是跨主题会翻转的填充（`--accent`、`--err`），
   其上的文字必须用配对的 `--on-*`。亮色主题下白字正确、暗色主题下白字
   可能不达标——`--on-err` 就是为此存在。
3. **半透明叠加用 `color-mix`**，不要写 `rgba(0,0,0,.08)` 这类固定值，
   它在暗色主题下不可见。
4. **动效可关。** 一切过渡都要在 `@media (prefers-reduced-motion: reduce)`
   下退化为 `none`。
5. **实例级变量必须带兜底。** 卡片按节点身份注入的 `--dot`（轮次色）、
   `--who` / `--who-text`（头像配色）由 `src/view.js`、`src/core.js` 写在
   行内 style 上，因此**在 `theme.css` 里查不到定义**，这是正常的。
   用到它们时必须写成 `var(--dot, var(--accent))` 形式，兜底值本身也取自 token。

第 1 条**由构建强制**，不是靠自觉：`scripts/check.js` 里有一条 `theme.css` 扫描，
任何出现在 token 块之外的 `#hex` / `rgb()` 都会让 `pnpm run build` 失败。

```bash
pnpm run build
# dsh-flow: theme.css has no literal colours outside its token blocks
```

（文档里写一句没人执行的命令，等于没写。这条规则曾经就是这样。）

---

## 5. 组件语言

- **卡片底盘** `.card`：描边 + 圆角，悬停加深描边，选中加 `--accent` 外环。
  没有左侧色条，没有彩色投影。
- **状态胶囊** `.chip--*`：`--X-soft` 底 + `--X` 字。任务态与团队态共用一套。
- **连线分级**：`--edge`（普通）/ `--edge-strong`（依赖，带箭头）/
  `--edge-active`（选中）/ 虚线（草稿、assign）/ `--accent` 实线（spawn、tick）。
  **任何缩放级别都有边**——不存在"放大才显示连线"的阈值。
- **检查器**：右栏，`--shadow-3`，`translateX` 进出场。
- **多智能体气泡** `.bubble`：头像列 + 正文列，`--panel-2` 底，
  用户气泡换 `--accent-soft` / `--accent-line`。语内分隔用 `.md-mark`。

---

## 6. 内容协议

画面上的"乱码"多数来自数据层，不是样式层。约定如下。

### 6.1 中继信封

成员之间经 DSH 会话中继的消息，原文形如
`Agent <uuid> sent a message:【发送者 → 接收者】正文`。
`src/relay.js` 提供解析与兜底清洗（宿主与客户端共用一份规格），
渲染层永远不应看到裸 UUID。

### 6.2 Markdown

`src/markdown.js` 是自写的合规解析器（delimiter stack），**不是正则近似**。

- 行内：`` ` `` `` ` `` 代码、`**粗**`、`*斜*`、`~~删~~`、`[文字](链接)`、
  `<https://…>` 自动链接、`\` 转义。嵌套与孤儿定界符按 CommonMark 的
  flanking 规则与 rule-of-three 处理；不成对的标记降级为字面量，
  而不是把整段吞掉。
- 块级：ATX 标题、围栏代码（``` / ~~~）、表格、引用、有序/无序列表（可嵌套）、
  分割线、段落（软换行渲染为 `<br>`）。
- **模型自用的分节符**（`■ □ ◆ ▲ ⇒`）在行首映射为 `.md-mark` 小节标题；
  单独一行时为 `<hr>`。白名单见 `SECTION_MARK`，是可配置项。
- **安全**：所有文本经 `escapeHtml`；链接只放行 `http(s):` / `mailto:` /
  站内相对路径，`javascript:`、`data:` 一律回退为字面量。模型输出是不可信输入。
- 未闭合的围栏按代码块渲染（流式输出中途不炸）。

### 6.3 与上游面板的边界

画布页用**属性前缀选择器**隐藏上游面板（`[class*="aYQbCq_"]`），
因为那是打包混淆过的类名，上游改版即失效。失败时只是多出一块面板，不会崩。
原生「对话」标签页不动——那是别人插件的合法地盘。
