{
  "name": "dsh-flow",
  "version": "0.3.0",
  "description": "One unified infinite canvas for DeepSeek Harness: conversation turns as a fork tree, agent teams as captain-member-task DAG clusters, and member messages rendered as a directed bubble stream in the inspector.",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js",
    "./client": "./client.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "index.js",
    "client.js",
    "engine.js",
    "src",
    "assets",
    "theme.css",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "build": "node --check index.js && node --check client.js && node --check engine.js && node --check canvas.js",
    "prepare": "pnpm run build",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-runtime"
      ],
      "immediately": false
    }
  }
}
